query GetPendingInvitations($input: GetPendingInvitationsInput!) {
	getPendingInvitations(input: $input) {
		_id
		business
		totalGuests
		names
		status
		tab
		requestor {
			_id
			name
			phoneNumber
		}
	}
}
