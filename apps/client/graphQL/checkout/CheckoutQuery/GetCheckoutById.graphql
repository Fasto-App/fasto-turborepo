query GetCheckoutByID($input: GetById!) {
	getCheckoutByID(input: $input) {
		_id
		splitType
		business
		created_date
		paid
		subTotal
		totalPaid
		total
		tip
		tax
		tab
		status
		payments {
			amount
			_id
			splitType
			patron
			tip
			discount
		}
	}
}
