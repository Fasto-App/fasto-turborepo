query GetSpacesFromBusiness {
	getSpacesFromBusiness {
		_id
		name
		business
		tables {
			_id
			status
			tableNumber
			tab {
				_id
				table {
					_id
					tableNumber
				}
				admin
				orders {
					_id
					status
				}
			}
		}
	}
}
