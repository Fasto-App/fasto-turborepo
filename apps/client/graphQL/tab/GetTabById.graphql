query GetTabByID($input: GetById!) {
	getTabByID(input: $input) {
		_id
		users {
			_id
		}
		_id
		table {
			_id
			tableNumber
		}
		admin
		orders {
			_id
			status
			quantity
			subTotal
			product {
				imageUrl
				price
				name
			}
		}
	}
}
