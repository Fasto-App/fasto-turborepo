mutation UpdateBusinessLocation($input: UpdateAddressInput!) {
	updateBusinessLocation(input: $input) {
		address {
			streetAddress
			stateOrProvince
			postalCode
			country
			complement
			city
			_id
		}
	}
}
