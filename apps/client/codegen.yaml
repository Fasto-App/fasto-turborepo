schema: http://localhost:4000/graphql
documents: './**/*.graphql'
generates:
    gen/generated.ts:
        plugins:
            - typescript
            - typescript-operations
            - typescript-react-apollo
        config:
            withHooks: true
